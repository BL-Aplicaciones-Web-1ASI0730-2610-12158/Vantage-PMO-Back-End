namespace vantagePMO_platform.Meetings.Domain.Model.Queries;

/// <summary>
/// Query que representa la intención de obtener todas las reuniones existentes.
/// No requiere parámetros porque getAllMeetings no recibe filtros desde el front-end.
/// </summary>
public record GetAllMeetingsQuery;
